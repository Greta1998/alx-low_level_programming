#include "main.h"

/**
 *_abs(int n)- prints the absolute value of n
 * @n: the int to be checked
 *
 * Return: Absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
